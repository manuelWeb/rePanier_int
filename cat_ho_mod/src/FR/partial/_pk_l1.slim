- nbpk = [1,2,3,4];
- cpt = 0; cpt1 = cpt+1;
table.mobile_300 width="612" border="0" cellpadding="0" cellspacing="0" align="center" style="margin: 0 auto;"
  - nbpk.each{ |i|
  / - if i == 1 || i == 3
  / - i.odd? return true si i est impaire
  - if i.odd?
    tr
      - if i % 2 == 1
        td.mob_d_b.pdb12 valign="top"
          table border="0" cellpadding="0" cellspacing="0"
            tr
              td.pdb16 align="center" width="#{$column*6+$gutter*5}"
                img.rz72 src==(($abspath == true ? $srcAbs : $srcRel)+"pk#{cpt+1}.jpg") alt=""
            tr
              td align="center"
                p.lib
                  | #{$putLib[cpt]}
            tr
              td align="center"
                p.price
                  | #{$putPri[cpt]}
        td.mob_d_none width="#{$gutter}"
          / | #{cpt}
        td.mob_d_b.pdb12 valign="top"
          table border="0" cellpadding="0" cellspacing="0"
            tr
              td.pdb16 align="center" width="#{$column*6+$gutter*5}"
                img.rz72 src==(($abspath == true ? $srcAbs : $srcRel)+"pk#{cpt+2}.jpg") alt=""
            tr
              td align="center"
                p.lib
                  | #{$putLib[cpt+1]}
            tr
              td align="center"
                p.price
                  | #{$putPri[cpt+1]}
    tr
      td height="12"

  - cpt += 1
  - }

  / - lib.each{ |j|
  / tr
  /   td
  /     p
  /      | j vaut #{j} index #{cpt}
  / - }